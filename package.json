{
  "name": "rankradar",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "setup": "npm install && cd client && npm install && cd .. && cd server && npm install && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@radix-ui/react-toast": "^1.2.15",
    "random-words": "^2.0.1",
    "uuid": "^13.0.0"
  }
}